// const Sequelize = require('sequelize');
// const db = require('../config/database');
// const Film = require('./films');

// // Film.belongsToMany(People, { foreignKey: 'filmId', through: 'ActorMovies' });
// // People.belongsToMany(Film, { foreignKey: 'peopleId', through: 'ActorMovies' });

// module.exports = People;
